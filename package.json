{
  "name": "pour",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply pour --local",
    "db:migrate:remote": "wrangler d1 migrations apply pour --remote",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3",
    "wrangler": "^4"
  }
}
